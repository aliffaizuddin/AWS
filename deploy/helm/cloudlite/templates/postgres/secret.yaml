apiVersion: v1
kind: Secret
metadata:
  name: postgres-credentials
type: Opaque
stringData:
  POSTGRES_PASSWORD: {{ required "postgres.password must be set — see deploy/helm/cloudlite/values-secrets.yaml.example" .Values.postgres.password | quote }}
  SPRING_DATASOURCE_PASSWORD: {{ required "postgres.password must be set — see deploy/helm/cloudlite/values-secrets.yaml.example" .Values.postgres.password | quote }}
