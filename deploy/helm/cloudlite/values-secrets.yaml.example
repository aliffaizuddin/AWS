# Copy this file to values-secrets.yaml (already gitignored — never commit that file)
# and fill in real values before installing. Pass it alongside values-dev.yaml:
#   helm install cloudlite deploy/helm/cloudlite -n cloudlite --create-namespace \
#     -f deploy/helm/cloudlite/values-dev.yaml -f deploy/helm/cloudlite/values-secrets.yaml
postgres:
  password: "CHANGE-ME"
iam:
  jwt:
    secret: "CHANGE-ME-at-least-32-bytes-for-a-real-deployment"
