apiVersion: v1
kind: Secret
metadata:
  name: iam-jwt-secret
type: Opaque
stringData:
  IAM_JWT_SECRET: {{ required "iam.jwt.secret must be set (>=32 bytes) — see deploy/helm/cloudlite/values-secrets.yaml.example" .Values.jwt.secret | quote }}
