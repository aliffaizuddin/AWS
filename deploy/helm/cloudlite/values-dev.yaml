# Local/dev overrides layered over values.yaml — never fork the chart
# per environment, per ADR 0006.
postgres:
  persistence:
    size: 2Gi
s3:
  persistence:
    size: 2Gi

prometheus:
  server:
    persistentVolume:
      size: 2Gi
