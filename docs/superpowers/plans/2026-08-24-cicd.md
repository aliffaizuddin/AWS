# CI/CD (Platform Layer, Part 3) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Give `services/s3` and `services/iam` path-triggered GitHub
Actions pipelines (test on every PR, build+push a `ghcr.io` image on
every push to `main`), plus a lint/template check for
`deploy/helm/cloudlite`, and prove all three work against the real
GitHub repo.

**Architecture:** One reusable `workflow_call` workflow
(`ci-java-service.yml`) implements the shared Java/Maven pipeline;
`ci-s3.yml` and `ci-iam.yml` are thin, path-triggered callers into it.
`ci-helm.yml` is independent — no registry, no reusable workflow, just
`helm lint`/`helm template` gated on two small committed fixture files
that satisfy the chart's `required()` secret guards.

**Tech Stack:** GitHub Actions (`workflow_call`, path filters),
`actions/checkout@v4`, `actions/setup-java@v4`, `docker/build-push-action@v6`,
`azure/setup-helm@v4`, Helm 3.

**Spec:** [`docs/superpowers/specs/2026-08-24-cicd-design.md`](../specs/2026-08-24-cicd-design.md)

## Global Constraints

- Registry: `ghcr.io`, namespace `ghcr.io/aliffaizuddin/aws/<service>`
  (all-lowercase — GHCR requires it), authenticated via the workflow's
  built-in `GITHUB_TOKEN`. No repository secret is created.
- Tag: short git SHA only (7 characters, e.g. `s3:a3f9c21`). Never a
  floating `latest` tag.
- On a pull request: `mvn test` only — no image is built or pushed. On
  a push to `main`: `mvn test`, then build and push the image. Push
  triggers are scoped to `branches: [main]` only — no other branch
  triggers a push-side run (PRs already cover every other branch's
  commits).
- JDK 21, Temurin distribution — matches `services/{s3,iam}/pom.xml`'s
  `<java.version>21</java.version>`.
- `ci-fnrunner.yml`/`ci-web.yml` are explicitly out of scope — neither
  service exists yet.
- Never auto-bump `deploy/helm/cloudlite/values.yaml`'s `image.tag`
  values or commit that back to `main` — that loop belongs to the
  ArgoCD sub-project, not this one.
- `ci-helm.yml` satisfies the chart's `required()` guards
  (`.Values.postgres.password` in `templates/postgres/secret.yaml`,
  `.Values.jwt.secret` in `charts/iam/templates/secret.yaml`) via
  small, committed, non-secret fixture files — never via `--set` flags
  scattered inside the workflow YAML.
- Workflow file names are exactly `ci-java-service.yml`, `ci-s3.yml`,
  `ci-iam.yml`, `ci-helm.yml`, all under `.github/workflows/`, per ADR
  0008.

---

### Task 1: Reusable Java-service workflow

**Files:**
- Create: `.github/workflows/ci-java-service.yml`

**Interfaces:**
- Produces: a `workflow_call` workflow named `ci-java-service.yml`,
  callable via `uses: ./.github/workflows/ci-java-service.yml`, with
  required string inputs `service-name`, `service-path`, `image-name`.
  Task 2 and Task 3 consume this.

- [ ] **Step 1: Create the reusable workflow file**

```yaml
name: CI - Java service (reusable)

on:
  workflow_call:
    inputs:
      service-name:
        description: "Short service name (e.g. s3, iam)"
        required: true
        type: string
      service-path:
        description: "Path to the service's Maven project (e.g. services/s3)"
        required: true
        type: string
      image-name:
        description: "Fully-qualified image name without a tag (e.g. ghcr.io/aliffaizuddin/aws/s3)"
        required: true
        type: string

permissions:
  contents: read
  packages: write

jobs:
  build-test-push:
    name: ${{ inputs.service-name }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven

      - name: Run tests
        working-directory: ${{ inputs.service-path }}
        run: mvn -B test

      - name: Compute short SHA
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        id: short-sha
        run: echo "sha=$(git rev-parse --short=7 HEAD)" >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: docker/build-push-action@v6
        with:
          context: ${{ inputs.service-path }}
          push: true
          tags: ${{ inputs.image-name }}:${{ steps.short-sha.outputs.sha }}
```

- [ ] **Step 2: Validate YAML syntax**

Run: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci-java-service.yml'))" && echo OK`
Expected: `OK` (no `yaml.scanner.ScannerError`/`yaml.parser.ParserError`)

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/ci-java-service.yml
git commit -m "feat(ci): add reusable Java-service workflow"
```

This workflow cannot run on its own (`workflow_call` has no caller
yet) — Task 2 gives it its first real caller and its first real test.

---

### Task 2: `ci-s3.yml`

**Files:**
- Create: `.github/workflows/ci-s3.yml`

**Interfaces:**
- Consumes: Task 1's `ci-java-service.yml` (`service-name`,
  `service-path`, `image-name` inputs).
- Produces: a path-triggered workflow watching `services/s3/**`. Task
  6 relies on this existing to validate real trigger behavior.

- [ ] **Step 1: Create the caller workflow**

```yaml
name: CI - s3

on:
  pull_request:
    paths:
      - "services/s3/**"
  push:
    branches: [main]
    paths:
      - "services/s3/**"

jobs:
  ci:
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/ci-java-service.yml
    with:
      service-name: s3
      service-path: services/s3
      image-name: ghcr.io/aliffaizuddin/aws/s3
```

- [ ] **Step 2: Validate YAML syntax**

Run: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci-s3.yml'))" && echo OK`
Expected: `OK`

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/ci-s3.yml
git commit -m "feat(ci): add path-triggered s3 CI workflow"
```

---

### Task 3: `ci-iam.yml`

**Files:**
- Create: `.github/workflows/ci-iam.yml`

**Interfaces:**
- Consumes: Task 1's `ci-java-service.yml`.
- Produces: a path-triggered workflow watching `services/iam/**`.
  Task 6 relies on this existing to validate isolation from `ci-s3.yml`.

- [ ] **Step 1: Create the caller workflow**

```yaml
name: CI - iam

on:
  pull_request:
    paths:
      - "services/iam/**"
  push:
    branches: [main]
    paths:
      - "services/iam/**"

jobs:
  ci:
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/ci-java-service.yml
    with:
      service-name: iam
      service-path: services/iam
      image-name: ghcr.io/aliffaizuddin/aws/iam
```

- [ ] **Step 2: Validate YAML syntax**

Run: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci-iam.yml'))" && echo OK`
Expected: `OK`

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/ci-iam.yml
git commit -m "feat(ci): add path-triggered iam CI workflow"
```

---

### Task 4: `ci-helm.yml` and its CI-only value fixtures

**Files:**
- Create: `.github/workflows/ci-helm.yml`
- Create: `deploy/helm/cloudlite/values-ci.yaml`
- Create: `deploy/helm/cloudlite/charts/iam/values-ci.yaml`

**Interfaces:**
- Consumes: the existing chart's `required()` guards —
  `templates/postgres/secret.yaml`'s `.Values.postgres.password` and
  `charts/iam/templates/secret.yaml`'s `.Values.jwt.secret` (top-level
  `jwt.secret` when `charts/iam` is rendered standalone).
- Produces: a path-triggered workflow watching `deploy/helm/**`. Task
  6 relies on this existing to validate the fail-then-pass demonstration.

- [ ] **Step 1: Create the umbrella-chart CI fixture**

```yaml
# deploy/helm/cloudlite/values-ci.yaml
# CI-only fixture: placeholder, non-secret values that satisfy this
# chart's Helm `required()` guards so `helm lint`/`helm template` can
# run without a real deploy/helm/cloudlite/values-secrets.yaml (which
# is gitignored and never committed). Never use these values for a
# real install.
postgres:
  password: "ci-lint-placeholder"
iam:
  jwt:
    secret: "ci-lint-placeholder-at-least-32-bytes-long"
```

- [ ] **Step 2: Create the iam-subchart CI fixture**

```yaml
# deploy/helm/cloudlite/charts/iam/values-ci.yaml
# CI-only fixture — see deploy/helm/cloudlite/values-ci.yaml's header
# for why this exists. Same purpose, but shaped for linting/templating
# this subchart standalone (its own values.yaml has `jwt.secret` at
# the top level, not nested under `iam:` the way the umbrella chart
# exposes it).
jwt:
  secret: "ci-lint-placeholder-at-least-32-bytes-long"
```

- [ ] **Step 3: Verify both fixtures actually satisfy the guards, locally**

Run:
```bash
helm lint deploy/helm/cloudlite -f deploy/helm/cloudlite/values-ci.yaml
helm template cloudlite deploy/helm/cloudlite -f deploy/helm/cloudlite/values-ci.yaml >/dev/null
helm lint deploy/helm/cloudlite/charts/s3
helm template s3 deploy/helm/cloudlite/charts/s3 >/dev/null
helm lint deploy/helm/cloudlite/charts/iam -f deploy/helm/cloudlite/charts/iam/values-ci.yaml
helm template iam deploy/helm/cloudlite/charts/iam -f deploy/helm/cloudlite/charts/iam/values-ci.yaml >/dev/null
```
Expected: all six commands exit 0, `helm lint` prints `0 chart(s)
failed` for each, no `execution error` from a `required` guard.

- [ ] **Step 4: Create the workflow**

```yaml
name: CI - Helm charts

on:
  pull_request:
    paths:
      - "deploy/helm/**"
  push:
    branches: [main]
    paths:
      - "deploy/helm/**"

jobs:
  lint-and-template:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Helm
        uses: azure/setup-helm@v4

      - name: Lint umbrella chart
        run: helm lint deploy/helm/cloudlite -f deploy/helm/cloudlite/values-ci.yaml

      - name: Template umbrella chart
        run: helm template cloudlite deploy/helm/cloudlite -f deploy/helm/cloudlite/values-ci.yaml

      - name: Lint s3 subchart
        run: helm lint deploy/helm/cloudlite/charts/s3

      - name: Template s3 subchart
        run: helm template s3 deploy/helm/cloudlite/charts/s3

      - name: Lint iam subchart
        run: helm lint deploy/helm/cloudlite/charts/iam -f deploy/helm/cloudlite/charts/iam/values-ci.yaml

      - name: Template iam subchart
        run: helm template iam deploy/helm/cloudlite/charts/iam -f deploy/helm/cloudlite/charts/iam/values-ci.yaml
```

- [ ] **Step 5: Validate YAML syntax**

Run: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci-helm.yml'))" && echo OK`
Expected: `OK`

- [ ] **Step 6: Commit**

```bash
git add .github/workflows/ci-helm.yml deploy/helm/cloudlite/values-ci.yaml deploy/helm/cloudlite/charts/iam/values-ci.yaml
git commit -m "feat(ci): add Helm lint/template workflow with CI-only value fixtures"
```

---

### Task 5: Platform doc

**Files:**
- Create: `docs/platform/cicd.md`

**Interfaces:**
- Consumes: Tasks 1-4's file list (for the Scope section) and this
  plan/spec's paths (for the doc's cross-links).
- Produces: nothing consumed by a later task — this is the sub-project's
  reference doc, per `CLAUDE.md`'s "docs/platform/ ... one file per
  sub-project" convention.

- [ ] **Step 1: Create the doc**

```markdown
# CI/CD

**Status:** built — path-triggered GitHub Actions workflows for
`services/s3` and `services/iam`, plus a Helm lint/template workflow
for `deploy/helm/cloudlite`. See
[`../superpowers/plans/2026-08-24-cicd.md`](../superpowers/plans/2026-08-24-cicd.md)
for what was built and
[`../superpowers/specs/2026-08-24-cicd-design.md`](../superpowers/specs/2026-08-24-cicd-design.md)
for the design.

## Scope

- `.github/workflows/ci-java-service.yml` — reusable `workflow_call`
  workflow: checkout, JDK 21 (Temurin) setup with Maven caching,
  `mvn -B test`, and (only on a push to `main`) build the service's
  existing `Dockerfile` and push it to `ghcr.io`, tagged with the
  short git SHA.
- `.github/workflows/ci-s3.yml` / `ci-iam.yml` — thin, path-triggered
  callers into the reusable workflow (`services/s3/**` /
  `services/iam/**`).
- `.github/workflows/ci-helm.yml` — path-triggered on
  `deploy/helm/**`: `helm lint` + `helm template` against the umbrella
  chart and both subcharts independently. Uses two small committed
  fixtures (`deploy/helm/cloudlite/values-ci.yaml`,
  `deploy/helm/cloudlite/charts/iam/values-ci.yaml`) to satisfy the
  chart's `required()` secret guards — these are placeholder,
  non-secret values, safe to commit, and unrelated to the real
  (gitignored) `values-secrets.yaml` used for actual installs.

## Registry and tagging

Images publish to `ghcr.io/aliffaizuddin/aws/<service>`, tagged with
the 7-character short git SHA of the commit on `main` that produced
them (e.g. `s3:a3f9c21`) — no floating `latest` tag. Authentication
uses the workflow's built-in `GITHUB_TOKEN`; no registry secret exists
in this repo.

## Trigger behavior

| Event | s3/iam workflows | helm workflow |
|---|---|---|
| PR touching `services/s3/**` or `services/iam/**` | `mvn test` only, no image push | doesn't fire |
| PR touching `deploy/helm/**` | doesn't fire | `helm lint`/`template` only |
| Push to `main` touching `services/s3/**` or `services/iam/**` | `mvn test`, then build+push | doesn't fire |
| Push to `main` touching `deploy/helm/**` | doesn't fire | `helm lint`/`template` only |

## Validated against

The real GitHub repository, not a sandbox stand-in — see this plan's
Task 6 for the exact validation branches/PRs used to confirm each
workflow fires only for its own path, that `mvn test` genuinely runs
each service's Testcontainers-backed integration suite, and that
`ci-helm.yml` genuinely fails on a broken chart value before passing
once fixed.

## Out of scope

`ci-fnrunner.yml`/`ci-web.yml` (neither service exists yet),
auto-bumping `deploy/helm/cloudlite/values.yaml`'s image tags (the
ArgoCD sub-project's job), any registry other than `ghcr.io`, image
signing/provenance attestation, and configuring GitHub branch
protection "required checks" (a one-time repo-settings action, not a
file in this repo).
```

- [ ] **Step 2: Commit**

```bash
git add docs/platform/cicd.md
git commit -m "docs: add CI/CD platform doc"
```

---

### Task 6: End-to-end validation against the real GitHub repo

**This task is executed by the controller directly, not dispatched to
an implementer subagent.** Every other task in this plan only writes
and commits local files; this task pushes branches and opens/merges/
closes pull requests against the real, shared GitHub repository (even
though the base branch is `feat/cicd`, not `main`) — an origin-facing,
network action, and this repo's established practice (see the Helm
chart and prior sub-projects) is that the controller — not a
subagent — performs every `git push`/`gh pr` action itself.

**Files:**
- Modify: `services/s3/Dockerfile` (adds one real, permanent comment
  line — see Step 2)
- Modify: `services/iam/Dockerfile` (same, see Step 3)

**Interfaces:**
- Consumes: Tasks 1-5, pushed to `origin/feat/cicd`.
- Produces: confirmation (recorded in the SDD ledger) that all three
  workflows fire correctly, in isolation, against the real repo — the
  deliverable the final whole-branch review will check for.

- [ ] **Step 1: Push the branch so GitHub can see the new workflows**

```bash
git push -u origin feat/cicd
```

- [ ] **Step 2: Validate `ci-s3.yml` fires, in isolation, on a real PR**

```bash
git checkout -b test/ci-s3-trigger feat/cicd
```

Add this one line as the first line of `services/s3/Dockerfile`
(a real, permanent comment explaining the multi-stage build's second
stage — not a throwaway edit):

```dockerfile
# Runtime stage: JRE only, no Maven/JDK — the jar is already built above.
FROM maven:3.9-eclipse-temurin-21 AS build
```

(i.e. insert the comment line immediately before the existing `FROM
maven:3.9-eclipse-temurin-21 AS build` line — the file's first line.)

```bash
git add services/s3/Dockerfile
git commit -m "docs(s3): explain the Dockerfile's runtime-stage split"
git push -u origin test/ci-s3-trigger
gh pr create --base feat/cicd --head test/ci-s3-trigger \
  --title "docs(s3): explain the Dockerfile's runtime-stage split" \
  --body "Validation PR for the CI/CD sub-project: confirms ci-s3.yml fires on a services/s3/** change and ci-iam.yml/ci-helm.yml do not."
```

Wait for checks, then inspect:

```bash
gh pr checks test/ci-s3-trigger
gh run list --branch test/ci-s3-trigger
```

Expected: exactly one workflow run, for `CI - s3`, with a green `mvn
-B test` step. No run for `CI - iam` or `CI - Helm charts` (neither
exists to trigger yet at this point — `ci-iam.yml` isn't merged into
`feat/cicd` until Task 3, so this run alone cannot yet prove isolation
from it; Step 3 below is the real isolation proof).

```bash
gh pr merge test/ci-s3-trigger --squash --delete-branch
git checkout feat/cicd
git pull
```

- [ ] **Step 3: Validate `ci-iam.yml` fires, in isolation from `ci-s3.yml`, on a real PR**

```bash
git checkout -b test/ci-iam-trigger feat/cicd
```

Add the equivalent comment as the first line of
`services/iam/Dockerfile`:

```dockerfile
# Runtime stage: JRE only, no Maven/JDK — the jar is already built above.
FROM maven:3.9-eclipse-temurin-21 AS build
```

```bash
git add services/iam/Dockerfile
git commit -m "docs(iam): explain the Dockerfile's runtime-stage split"
git push -u origin test/ci-iam-trigger
gh pr create --base feat/cicd --head test/ci-iam-trigger \
  --title "docs(iam): explain the Dockerfile's runtime-stage split" \
  --body "Validation PR for the CI/CD sub-project: confirms ci-iam.yml fires on a services/iam/** change and ci-s3.yml/ci-helm.yml do not."
gh pr checks test/ci-iam-trigger
gh run list --branch test/ci-iam-trigger
```

Expected: exactly one workflow run, for `CI - iam`, green. No run for
`CI - s3` or `CI - Helm charts` — this is the real isolation proof,
since `ci-s3.yml` now exists on `feat/cicd` (merged in Step 2) but
this PR touches only `services/iam/**`.

```bash
gh pr merge test/ci-iam-trigger --squash --delete-branch
git checkout feat/cicd
git pull
```

- [ ] **Step 4: Validate `ci-helm.yml` genuinely fails, then genuinely passes**

```bash
git checkout -b test/ci-helm-trigger feat/cicd
git rm deploy/helm/cloudlite/values-ci.yaml
git commit -m "test: temporarily remove the CI values fixture (validation only)"
git push -u origin test/ci-helm-trigger
gh pr create --base feat/cicd --head test/ci-helm-trigger \
  --title "test: ci-helm.yml failure/recovery validation (not to be merged)" \
  --body "Validation PR for the CI/CD sub-project: confirms ci-helm.yml genuinely fails without the CI fixture, then genuinely passes once it's restored. Will be closed without merging."
gh pr checks test/ci-helm-trigger
```

Expected: `CI - Helm charts` run is red — `helm lint`'s "Lint umbrella
chart" step fails with a `required` guard error (`postgres.password
must be set...`).

```bash
git revert --no-edit HEAD
git push
gh pr checks test/ci-helm-trigger
```

Expected: `CI - Helm charts` run is now green — same commit content as
`feat/cicd`, so this closes the loop.

```bash
gh pr close test/ci-helm-trigger --delete-branch
git checkout feat/cicd
```

- [ ] **Step 5: Record the outcome**

In the SDD ledger for this plan (`.superpowers/sdd/2026-08-24-cicd/progress.md`),
append a completion note under Task 6 summarizing: the three PR
numbers used, confirmation that each workflow fired only for its own
path, and that `ci-helm.yml` genuinely failed then genuinely passed.
Note explicitly that the push-to-main build+push path (image actually
landing in `ghcr.io`) is NOT validated by this task — `feat/cicd`
itself hasn't merged to `main` yet, and none of the validation PRs
targeted `main`. That check happens once this sub-project's real PR
is approved and merged to `main` (the single merge event this whole
SDD cycle already ends with) — the controller runs `gh run list
--branch main --workflow=ci-s3.yml` and the `iam` equivalent
immediately after that merge, and confirms
`ghcr.io/aliffaizuddin/aws/{s3,iam}:<merge-commit-short-sha>` are
pullable, exactly as was done for the Helm chart PR's post-merge branch
sync.
